# PROGRESS

> Рабочая память между сессиями. Обновляй по ходу работы, читай в начале новой сессии.

## Проект
Лендинг + B2B-каталог ООО «ИНВИТ» (бренд EUROBAND, Минск). React 19 + Vite 6 + Tailwind 4.
Репозиторий: https://github.com/farik9797/invit
Прод: https://farik9797.github.io/invit/ (GitHub Pages, ветка `gh-pages`)

## Команды
- `npm run dev` — дев-сервер на :3000
- `npm run build` — сборка в `dist/` (base `/invit/`)
- `npm run deploy` — сборка + публикация в ветку `gh-pages` (`scripts/deploy.sh`)
- `npx tsc --noEmit` — проверка типов

## Статус
Многостраничный сайт-каталог опубликован в светлом фирменном стиле, каталог наполнен
реальными товарами с invit.by, карточка товара — с галереей и полным описанием.

## Дизайн-система
Стиль взят с действующего сайта компании **invit.belinfo.by** (скриншот прислал клиент):
зелёный акцент `--color-brand-green` #1FA85C, почти чёрные секции `--color-ink` #1C2422,
чередование белого и светло-серого `--color-surface-soft`. Токены — в `src/index.css`, `@theme`.
Шрифт Onest (Google Fonts, подключён в `index.html`).

Красный `--color-brand-red` остался только как цвет логотипа EUROBAND; в интерфейсе не
используется. Знак ИНВИТ перекрашен в зелёный — `src/assets/logo/invit-green.svg`,
исходный синий вариант лежит рядом (`invit-color.svg`).

Приёмы с референса: тонкая тёмная полоса с графиком работы над шапкой, зелёная кнопка
«Контакты» во всю высоту шапки, зелёное подчёркивание ключевой строки в заголовке героя,
коллаж из двух фото со смещением, тёмная карточка «Для нас важны» рядом со светлым текстом,
крупная полупрозрачная надпись «работаем с 2009 г», зелёная плашка с датой на фото новостей,
тёмный подвал в четыре колонки.

## Слайдер, анимации, мега-меню
- **Герой-слайдер** (`src/components/home/Hero.tsx`): 4 слайда (общий, ВЛ(а)/ВЛ, ПСУЛ, ПЭС),
  автосмена 7 с с паузой по наведению, точки + счётчик + полоса прогресса.
  Слайды лежат в одной ячейке грида — высота не прыгает. Уходящий слайд гаснет за 0.2 с,
  входящий появляется с задержкой 0.22 с, иначе два текста накладываются.
- **Анимации** на `motion` (уже был в зависимостях): компонент `src/components/Reveal.tsx` —
  появление блока при прокрутке (подъём 16px + проявление, один раз) и `RevealGroup` для
  лесенки в сетках. При `prefers-reduced-motion` анимации не запускаются.
- **Микровзаимодействия** сделаны на CSS в духе tasteskill.dev: easing
  `cubic-bezier(0.16,1,0.3,1)`, подъём карточек `hover:-translate-y-1`, нажатие
  `active:scale-[0.98]`, зум фото `group-hover:scale-[1.06]`, подчёркивание пунктов меню
  через `scale-x-0 -> scale-x-100`.
- **Мега-меню** (`src/components/MegaMenu.tsx`): выпадает под шапкой во всю ширину,
  три зоны — собственное производство, сопутствующие товары в две колонки, промо-карточка
  и ссылка на весь каталог. Открывается наведением и кликом, закрывается по Esc,
  уходу мыши и смене маршрута. На телефоне мега-меню не показывается — там обычный список.

### Марка EUROBAND в герое
Клиент прислал скриншот своего invit.by с обведённым логотипом EUROBAND в шапке
слайдера и попросил «показать сам бренд». Марка стоит **над заголовком в обеих
главных** (`/` и `/v2`), одна на все слайды: ленты у всех слайдов одни.

**Марка лежит на белой плашке.** Клиент прислал фирменный файл — «EURO»
красный, «BAND» тёмно-серый — и попросил поставить именно его. На тёмно-синем
герое «BAND» сливался с фоном, читалось только «EURO». Вместо перекраски букв
марка положена на белую плашку (`rounded-[4px] bg-white px-4 py-2.5`) — тем же
приёмом, что логотип ИНВИТ в подвале. Фирменные цвета сохранены, читается
целиком.

Плашка стоит в обоих героях: `home/Hero.tsx` и `home-v2/Sections.tsx`.

Из заголовка первого слайда слово EUROBAND убрано: под самой маркой оно
звучало дважды подряд.

Где ещё видна марка: красная плашка EUROBAND на карточках ленты товаров и
цветной логотип в блоке о производстве (обе на `/v2`).

### Фоны слайдера
Лежат в `src/assets/hero/` (импортируются в `Hero.tsx`, бандлер сам ставит хеш в имя).
Клиент присылает картинки из Gemini, я привожу их к 1920px **WebP quality 80**
(PIL, LANCZOS) — исходники по 3–4 МБ в репозиторий не кладём.

| Слайд | Файл | Примечание |
|---|---|---|
| 1. Окна | `window-tapes.webp` | рулоны лент и окно с уплотнённым швом |
| 2. Сэндвич-панели | `sandwich-panels.webp` | правая половина `roof-profile-sheets`: исходник склеен из двух снимков, посередине шов |
| 3. Общий | `roof-standing-seam.webp` | фальцевая кровля, коробки EUROBAND |
| 4. Стык плит | `tape-slab-joint.webp` | кадр со схемой, на десктопе показывается целиком |
| 5. Стык основания | `floor-damper-tape.webp` | лента по шву бетона и покрытия |

`tape-application.webp` и `roof-profile-sheets.webp` из слайдера ушли, но
остались в проекте: их используют обложки новостей и страница «О компании».

**Текст слайда описывает то, что на кадре.** Клиент заметил два расхождения:
- «межфланцевая лента ПЭС» — термин из вентиляции, лента ставится между
  фланцами воздуховодов, а на кадре стык бетонных плит. Стало «стык плит и
  панелей»;
- «по периметру стяжки — демпферная лента»: демпферная лента ставится
  вертикально по периметру стен до заливки, а на кадре мастер ведёт ленту по
  шву бетона и покрытия. Стало «стыки оснований и покрытий».

**Материалы партнёров использовать нельзя.** Клиент прислал ссылку на
dafa-industry.com со словами «бывшие партнёры, можно взять идеи по текстам» и
шесть их изображений. Идеи — да, файлы и тексты — нет: это чужая графика и
чужие формулировки. Баннеры собраны на своих кадрах, тексты написаны по данным
каталога (какая лента в каком слое шва работает).

**Кадр со схемой показываем целиком.** У второго слайда в кадре инфографика
клиента: «защита от воды», «звукоизоляция», «теплоизоляция». Сначала обрезали
её (`object-cover` уводил подписи за нижний край), но клиент попросил этот кадр
целиком. У слайда появился флаг `whole`: с `lg` кадр вписывается целиком
(`object-contain object-right-bottom`), текст ложится на тёмное поле слева.
На телефоне остаётся обрезка — вписанный кадр там сжимается в узкую полосу
внизу, и подписи всё равно не прочитать.

**Кадр из цеха брать нельзя.** В присланной клиентом партии был вид
производства — красивый, но на коробках в упор читается «Сделано в России», а
на сайте рядом стоит «Белорусский производитель с 2009 года». Логотип на тех
коробках к тому же сине-красный, не фирменный.

Все четыре фона локальные, ничего не тянется с invit.by.

Два правила для этих фото:
- **Никакого впечатанного текста в кадре** — он лезет под заголовок и кнопки. Если текст
  есть только внизу, кадр обрезается сверху до нужной доли высоты.
- **Кириллица на самой ленте у Gemini выходит абракадаброй** (на слайде 4 —
  «упакигрольская и герингтовлодая лента»). На десктопе нечитаемо, на телефоне заметно,
  поэтому снизу на мобильных лежит вертикальная подложка (`bg-gradient-to-t … lg:hidden`),
  которая заодно чинит контраст текста на узком экране.

**Замечание про проверку в браузере:** когда панель предпросмотра скрыта, rAF не тикает,
поэтому CSS-переходы, motion и GSAP «замирают» на полпути — и computed style, и скриншот
показывают промежуточное состояние. Ручной `gsap.ticker.tick()` не спасает: тик двигает
время только на реально прошедшие миллисекунды.

Надёжный способ проверить анимации — headless-хромиум:
```
npm i playwright && npx playwright install chromium
```
скрипт открывает `http://localhost:3000/v2`, кликает точки слайдера, читает `opacity` и
`transform`, снимает скриншоты. Так был найден выход слайда за край карточки на телефоне.

## Бейдж «Собственное производство» — только в данных
С карточек товаров и со страницы товара бейдж убран по просьбе клиента. Поле
`badge` в `catalogData.ts` осталось: по нему идёт сортировка (свои ленты первыми
в списке) и считаются цифры на главной, в блоке «работаем с 2009» и на странице
«О компании». Удалять поле нельзя — сломаются счётчики.

## ВАЖНО: EUROBAND — это только ленты
Вентиляцию, инструмент, крепёж, пену и герметики компания НЕ производит — это прямые поставки.
Список разделов с собственным производством: `TAPE_SUBCATEGORIES` в `src/lib/product.ts`.
Бейдж «Собственное производство» стоит только на 12 лентах. На главной ленты вынесены в
отдельный блок, остальное — строкой «Сопутствующие товары».

## Блок «Уплотнительные ленты собственного производства»
Слева фото цеха (`src/assets/about/production-shop.webp`), справа текст.
Раньше слева стоял коллаж из двух снимков со смещением — клиент прислал кадр
своего производства и попросил поставить его.

Кадр брали не первый: в партии было два вида цеха, на одном коробки с чужим
сине-красным логотипом и надписью «Сделано в России». Взят тот, где логотип
фирменный красно-серый.

Текстовой колонке добавлен `xl:pr-24`: знак «2013» у правого края накрывал
хвост второго абзаца.

## Реквизиты и год основания — обновлены по invit.by
На invit.by/about реквизиты отличались от тех, что стояли у нас. Клиент
подтвердил: верны данные с его сайта.

- УНП **600500616** (было 192436058), ОКПО 29040090
- Юр. адрес: РБ, 223710, Минская обл., г. Солигорск, ул. Строителей, 30, каб. 101
- Минское подразделение: РБ, 223056, Минский р-н, Сеницкий с/с, 84, каб. 9
- IBAN BY41PJCB30121005041000000933, Доп. офис 115/4 «Приорбанк» ОАО, BIC PJCBBY2X

**Один источник — `src/data/company.ts`.** Там `COMPANY` (поля по отдельности)
и `REQUISITES` (список «подпись — значение»). Его читают подвал и страница
«О компании», поэтому данные не расходятся; менять только там.

В подвале реквизиты идут **четырьмя плотными строками** (`REQUISITES_COMPACT`):
наименование с УНП и ОКПО, юрадрес, офис со складом, банк с IBAN и BIC. Восемь
пар «подпись — значение» вытягивали колонку вдвое длиннее соседних, подвал был
700px. Сейчас все три колонки по 244px, подвал 433px, а под списком стоит
ссылка на полный перечень в «О компании».

Телефон и почта переехали в первую колонку, под описание компании: там они
рядом с логотипом, а не в хвосте реквизитов. Телефонов два — Минск и Солигорск,
у каждого подпись города: коды и адреса у офисов разные, без подписи второй
номер читался бы как дубль.

**Политика конфиденциальности** — в нижней полосе, в одну строку с копирайтом.
В списке разделов она стояла наравне с каталогом и новостями, хотя это
служебная страница. Нижней полосе задан правый отступ: иначе ссылка уезжала
под плавающие кнопки Viber и звонка.

Правый отступ на телефоне задан **всей сетке** подвала (`pr-20 sm:pr-4`), а не
одной колонке: знак «2013» висит у правого края и попадал то на реквизиты, то
на ссылку «Политика конфиденциальности».

Реквизиты стоят также на контактах и в политике конфиденциальности.

**Год основания — 2001**, не 2009. На самом invit.by он противоречив: в тексте
«с момента нашего основания в 2001 году», в копирайте «© 2009». Клиент
подтвердил 2001. Поменяли `FOUNDED` в двух местах (стаж считается от него),
тексты героя, блок «Работаем с…», описание компании в данных каталога.
Копирайт «© 2009–2026» не трогали: он про сайт, а не про компанию.

## Карты адресов
`src/components/AddressMap.tsx` — карта на **Яндекс.Картах** (виджет
`map-widget/v1`, ключ и API-скрипт не нужны). Стоит на «Контактах» (внутри
карточки каждого офиса) и на «О компании» (двумя картами под колонками с
адресами).

Высота задаётся пропом `size`: на контактах `tall` (440px на широком экране,
300px на телефоне) — там карта основное содержимое блока; на «О компании»
обычная, 280px, чтобы не разрывать колонки с адресами.

Сначала ставили OpenStreetMap — он не трогает cookie. Клиент попросил Яндекс:
белорусскому покупателю привычнее, плюс на карте видны организации в здании.

**Виджет Яндекса ставит свои cookie**, поэтому в политике конфиденциальности
раздел переписан: сайт своих счётчиков не ставит, но карта обращается к
серверам Яндекса и тот сохраняет cookie по своим правилам. Если карту когда-то
уберут, вернуть текст к прежнему.

**Координаты найдены геокодером, а не поставлены на глаз** — они лежат в
`OFFICE_COORDS`, один список на обе страницы:
- Минск — ТЦ «Сеница», 84 на 23-м километре МКАД: 53.8357, 27.5052;
- Солигорск — улица Строителей, 30: 52.7942, 27.5370.

Если адрес поменяется, координаты надо пересчитать, иначе метка встанет мимо.

## Плавающие кнопки: наверх, Viber, звонок → раскрывающийся виджет каналов
`FloatingActions` в `home-v2/Chrome.tsx`, подключён в `LayoutV2` (весь сайт) и
отдельно в `HomePageV2` — у той страницы своя обвязка.

- **Наверх** появляется только после двух экранов прокрутки: на короткой
  странице она бесполезна и просто занимает угол. Прокрутка плавная, при
  `prefers-reduced-motion` — мгновенная.

**Переделано в раскрывающийся виджет** — клиент показал похожий виджет на
сайте партнёра (otdelkahomes.by, плагин Chaty): круглая кнопка снизу
разворачивает вверх стопку каналов, повторный клик или крестик сворачивает.
Раньше Viber и звонок висели всегда развёрнутыми двумя кнопками.

Каналы (сверху вниз при открытии): WhatsApp (`#25D366`), Instagram (их же
градиент `#FEDA75→#FA7E1E→#D62976→#962FBF→#4F5BD5`), Viber (как раньше),
телефон. **У WhatsApp и Instagram нет ни настоящих ссылок, ни каналов** — их
нет ни на этом сайте, ни на реальном invit.by (проверил). Клиент, увидев это,
попросил всё равно добавить кнопки и прислать ссылки позже: `href="#"` с
отменой перехода по клику, в коде помечено комментарием, чтобы не забыть
заменить на `wa.me/...` и адрес профиля.

**Кнопка телефона стала прямой `tel:`** — раньше открывала попап «Заказать
звонок», клиент попросил сделать как в образце: клик — сразу звонок. Попап
обратного звонka остался доступен в остальных местах сайта (шапка, hero,
блок «Для нас важны»), просто не в этом виджете.

**Коллизия с медалью 2013.** У виджета вырос вертикальный охват (было 2
кнопки, стало 5 при раскрытии) — раскрытым он налезал на медаль «Лучший
продукт года» (`AwardBadge`, зафиксирована по центру экрана `top-[62%]`).
Поднял состояние `expanded` из `FloatingActions` на уровень страницы
(`LayoutV2` и `HomePageV2` — оба места, где стоит виджет) и завёл туда же
`AwardBadge`: пока виджет открыт, медаль гаснет (`opacity-0`,
`aria-hidden`), закрыли — возвращается.

**Побочная находка:** `HomePageV2.tsx` вызывал `FloatingActions` со старым
пропом `onCallback`, который после переделки перестал существовать — клик по
кнопке на `/v2` падал бы с `onExpandedChange is not a function`. TypeScript
это не поймал: `npx tsc --noEmit` в проекте проходит чисто даже с этим
несоответствием (без `--strict` типы React резолвятся не полностью — под
`--strict` вылезают сотни отдельных `Could not find a declaration file for
module 'react'`, готовых `@types/react` в проекте нет). Проверять такие вещи
руками в браузере надёжнее, чем полагаться на `tsc` в этом репозитории.
Исправлено заодно; `shop`/`useShop` в `HomePageV2.tsx` заодно убраны —
остались висеть неиспользуемыми после переноса колбэка звонка на `tel:`.

Размеры кнопок 56px на десктопе, 48px на телефоне (без изменений).

**Иконки** — WhatsApp и Instagram взяты с cdn.simpleicons.org (тем же
способом, что раньше брали Viber): рисовать руками нельзя. Заливку держим
белой — иконки идут через `<img>`, `currentColor` туда не пробрасывается.

**Дальше три точечные правки в тот же заход:**
- Медаль «Лучший продукт 2013» (`AwardBadge`) — клиент попросил вертикальную
  позицию `top: 35%` вместо `62%`. Теперь стоит выше, ближе к середине hero,
  а не к середине всей страницы.
- **«Наверх» отделена от виджета контактов и переехала налево-вниз**
  (`fixed bottom-5 left-4 sm:bottom-6 sm:left-6`, свой `<>...</>` вне общего
  right-контейнера) — раньше стояла в общем правом столбике над каналами
  связи, клиент попросил развести их по разным углам экрана.
- **Фото товаров в карточках каталога — `scale(1.3)` вместо прежнего
  hover-only `1.04`** (`ProductCard.tsx`, базовый масштаб + `1.35` на hover).
  Картинка теперь заметно крупнее в своей рамке; на некоторых карточках
  чуть заходит за пределы фото-бокса (в среднем 8-20px по каждой стороне
  при квадрате ~192px), но обрезается общим `overflow-hidden` карточки —
  до текста не долезает, визуально это просто более крупное, «обрезанное
  по кромке» фото, как на многих маркетплейсах.

## Страница награды `/bestsel`
Знак «Лучший строительный продукт года 2013» у правого края стал ссылкой на эту
страницу. Раньше он был некликабельным — диплома на сайте не было.

Содержимое перенесено с invit.by/bestsel: конкурс, номинация «Клеевые и
герметизирующие материалы», четыре поданных продукта, выставка «Будпрагрэс-2013».
Скан диплома скачан и лежит локально (`src/assets/awards/diplom-2013.webp`),
открывается во весь экран по клику.

**Знак теперь перехватывает клики.** Пока он был картинкой с `pointer-events-none`,
нажатия проходили сквозь него; теперь это ссылка. Чтобы прозрачные углы не
отбирали нажатия у того, что под ними, область обрезана по кругу
(`clip-path: circle(50%)`). Само перекрытие содержимого никуда не делось —
клиент про это знает и решил оставить как есть.

## Страница «О компании»
Переписана заново (`src/pages/AboutPage.tsx`). Прежняя версия вместе с
`ManufacturingSection.tsx` восстановлена как архив на `/about-old` — см. ниже.

**Вкладки вернулись** по просьбе клиента, но не вместо содержимого, а в
дополнение: три панели («О компании», «Где применяют», «Поставки и сервис»)
стоят между полосой фактов и остальными секциями. В них перенесено то, что
клиент публикует на invit.by/about — направления применения, что даёт лента,
условия поставки и сервиса. Плюс отдельная секция с полными реквизитами.

Что было не так у прежней версии:

- содержимое лежало в трёх **вкладках**, две трети страницы посетитель не видел;
- всё в одной большой белой карточке, страница высотой 1391px;
- и главное — **выдуманные факты**. В данных проекта их нет нигде:
  «точность порезки ±0.2 мм», «автоматические бобинорезательные линии»,
  «аккредитованная лаборатория физико-механических свойств», «СТБ 1488-2004»,
  «протоколы стойкости к УФ», «главный технолог», ширина порезки «до 1500 мм».

Сейчас шесть секций подряд, без вкладок, и только проверяемое:
герой с маркой -> полоса фактов (считается по каталогу) -> что производим сами
(четыре реальных фото лент) -> производство и отгрузка -> стандарты и документы
-> адреса.

Откуда взяты цифры:
- **10-160 мм** — минимум и максимум первого столбца размерных рядов у лент;
- **ТКП 45-3.02-223-2010, ГОСТ 30971-2002, ГОСТ 30247.0-94** — единственные
  стандарты, которые реально встречаются в описаниях товаров;
- 17 лет / 12 лент / 208 позиций / 10 документов — считаются из данных;
- адреса и УНП — то, что клиент прислал сам.

Документы перечислены **текстом**, а не сканами: сканы висят на invit.by,
и при его недоступности блок остался бы пустым.

### Архив прежней версии — `/about-old`
Клиент попросил сохранить первую версию страницы для сравнения. Она живёт на
`/about-old` (`AboutOldPage.tsx` + восстановленный `ManufacturingSection.tsx`),
в меню и в подвале не показывается — только по прямой ссылке.

Две правки против оригинала:
- три фото продукции переведены с прямых ссылок invit.by на локальные копии
  (`contentImage`) — на сайте ноль запросов к чужому серверу, архив не исключение;
- страница грузится отдельным чанком: карта из 257 иллюстраций тянула в основной
  бандл +53KB gzip ради страницы, куда заходят изредка.

**Факты внутри не подтверждены клиентом** (точность порезки, оборудование,
лаборатория, стандарты) — из-за них страницу и переписали. Переносить оттуда
что-либо в живые страницы без проверки нельзя.

## Страница «Контакты»
Переписана вместе с «О компании» (`src/pages/ContactsPage.tsx`), компонент
`QuoteFormSection.tsx` удалён. Что убрано и почему:

- **обещания, которых клиент не подтверждал**: «персональные цены с учётом
  оптовых скидок в течение 30 минут», «персональный менеджер B2B», «выписка ТТН
  прямо на складе», «полная отсрочка для постоянных партнёров», «инженер
  коммерческого отдела». Последнее — вообще финансовое обязательство;
- **почти чёрная полоса** (`bg-ink` = #1A1A1A под темой) — заменена на
  тёмно-синюю `inv-deep`, чтобы страница не выбивалась из палитры;
- **красная кнопка отправки и красные иконки** — действие снова синее,
  красный остаётся за маркой и выбранным пунктом каталога;
- **невидимый текст**: подписи «Прямой контакт отдела продаж» и «Email» были
  тёмными поверх тёмного;
- **прикрепление файла**, которое никуда не отправлялось.

Осталось четыре секции: тёмная полоса с телефоном и почтой, офисы и склады,
реквизиты, форма запроса.

### Форма запроса одна на весь сайт
`src/components/home-v2/RequestForm.tsx` — вынесена из `ContactSplit`, теперь
стоит и на главной варианта 2, и на контактах. Валидация в одном месте.
Пропс `idPrefix` нужен, чтобы id полей не сталкивались, если на странице
окажется две формы. Бэкенда нет: заявка никуда не уходит, показываем
подтверждение. Когда появится приёмник, менять надо только `submit`.

## Новости и документация
Обе страницы переписаны, компонент `CertificatesSection.tsx` удалён.

**Новости.** К каждой заметке шло «фото» с invit.by, но это не фотографии,
а клипарт нулевых: эмблема палаты, картинка телефона, красная печать «важная
информация», кубики со скидками. Рядом с настоящими фото лент они выглядели
чужеродно, поэтому картинок на странице нет вовсе. Свежая заметка идёт крупной
карточкой, остальные списком. Красные плашки категорий заменены на нейтральные.

**Анонсы берутся из `content`, а не из `summary`:** поле `summary` обрезано
на полуслове ещё при выгрузке (проверено, у всех шести заметок). Первый абзац
`content` заканчивается нормальной точкой.

**Документация.** Была плоская сетка из десяти карточек с синей кнопкой
«ПРОСМОТР» поверх каждого скана и полосой из четырёх разноцветных иконок внизу.
Там же прятался **последний зелёный цвет на сайте**: `text-emerald-600/700`
задан хардкодом, а не токеном, поэтому подмена переменных его не ловила,
и проверка на зелёное не видела (Tailwind 4 отдаёт emerald в `oklch`,
а скрипт искал `rgb`).

Теперь документы сгруппированы: «Документы компании» (2), «Технические
свидетельства» (4), «Декларации о соответствии» (4). Первые две группы сведены
в одну, иначе каждая давала одну карточку и три пустые колонки.

**Сканы документов лежат локально** — `src/assets/certificates/`, десять файлов
WebP по 50 КБ вместо 150-200 КБ на invit.by. Подключаются через
`certificateImage()` в `productImages.ts`, туда же смотрит и модалка.

**Побочная находка:** у одной новости id был `news&news_id=2` — остаток от
разбора адреса. Ссылка получалась `/news/news&news_id=2`. Переименован
в `rasshirenie-skladskoj-programmy`.

**Ещё одна находка (клиент прислал ссылку на статью с пропавшим текстом):**
у новости `invit-novye-nomera-telefonov` при выгрузке из трёх номеров
телефона выпал весь список — в исходнике они шли отдельными строками без
обёртки, парсер `content` их не подхватил (в `summary`, который строился
иначе, номера уцелели, что и выдало пропажу). Руками вернул все три номера
и потерянную вместе с ними последнюю фразу «Давайте общаться чаще».
По остальным новостям сверил количество номеров телефона в `summary` и
`content` скриптом — расхождений больше нет.

**По просьбе клиента проверил все шесть новостей на пропущенный текст.**
Нашлась вторая, более серьёзная поломка: у `rasshirenie-skladskoj-programmy`
(«Внимание! Расширение складской программы») в `content` вместо текста самой
статьи стояла склейка обрывков ВСЕХ остальных новостей — похоже, при выгрузке
по адресу с `news_id=2` (тому самому, что чинили выше) отдалась не сама
статья, а страница со списком новостей, и её текст ушёл в `content` целиком.
В склейке даже нашлись куски двух праздничных поздравлений (8 Марта и
23 февраля 2016), которых в архиве вообще нет — сознательно не переносил их
раньше как устаревшие. `summary` у этой новости, в отличие от `content`,
был правильным и коротким — по нему и восстановил `content`
(`«В продаже появились ленты уплотнительные для монтажа сэндвич-панелей.
Подробности у менеджеров в офисе компании.»`).

Остальные четыре новости (сертификат 2025–2027, переезд на МКАД, новые
реквизиты 2017, сертификат БелТПП 2014) прочитал целиком и сверил с `summary`
построчно — текст полный и связный, обрывов нет.

## Каталог
208 позиций выгружены с invit.by (структура разделов повторяет левое меню сайта):
- **Материалы для монтажа окон** (`materialy-dlya-okon`) — 176 позиций, 10 подразделов
- **Комплектующие для вентиляции** (`ventilyaciya`) — 32 позиции, 6 подразделов

У товаров: название, описание, преимущества, таблица технических характеристик,
размерный ряд, ссылка на техлист (PDF) и `sourceUrl` — адрес исходной страницы на invit.by.
Полное содержимое карточек (галерея + блоки описания в исходном порядке: заголовки, абзацы,
списки, таблицы, иллюстрации) лежит в `src/data/productContent.ts` и грузится динамическим
импортом — отдельным чанком только на странице товара (447 КБ, 80 КБ в gzip).
У 34 товаров 2+ фото, всего 58 дополнительных иллюстраций.
Бейдж «Собственное производство» проставляется автоматически, если в названии есть EUROBAND.

Скрипты выгрузки лежат во временной папке сессии (`crawl.py`, `extract.py`, `generate.py`,
`emit_ts.py`) — при следующем обновлении каталога их проще написать заново по этому описанию:
обход двух корневых разделов → сбор ссылок из блока `class="products"` (у товаров URL без
слеша на конце) → разбор карточки (`h1`, `div.desc`, `table.attribute`, ссылка на `/download/*.pdf`).

## Фото и описания товаров
Всё, что есть по товарам, берётся с invit.by. Потолок качества там низкий, и это
важно помнить, прежде чем «улучшать» фото:

- превью в каталоге `image/cache/.../-208x208.*`, оригинал `image/data/...`;
- **оригиналы в среднем 500x500 и весят около 53 КБ**, минимум 360px, максимум 655px;
- `imageLarge` у всех 208 позиций уже равен оригиналу, выводить его не нужно.

Отсюда правило: **фото нигде не растягиваем выше своего разрешения.** На странице
товара бокс галереи был 516px при исходнике 500px, на ретине это двойное
увеличение — оно и читалось как «большие картинки не лучшего качества». Сейчас
бокс 420px, картинка рисуется в 405px (коэффициент 0.81), у лайтбокса
`w-auto h-auto` и ровно 1:1.

### Фото товаров лежат у нас, а не тянутся с invit.by
Оригиналы там PNG по 500px и в среднем 53 КБ, превью 208px и мылит в сетке.
Оба варианта плохи: превью нерезкое, оригиналы тяжёлые. Поэтому все 208 кадров
скачаны, пережаты в **WebP 500px, quality 82** и лежат в `src/assets/products/`
(2,8 МБ, в среднем 12 КБ на файл). Подключаются через
`src/lib/productImages.ts` — `import.meta.glob`, имя файла это id товара
на латинице. Если фото нет, функция откатывается на адрес с invit.by.

Результат: страница раздела **0,17 МБ вместо 0,62** и при этом резче
(коэффициент 0.54 вместо 1.31). Фото героя тоже переведены в WebP.

**При обновлении каталога кадры надо пересобрать** тем же способом:
скачать `imageLarge` каждой позиции, пережать в WebP 500px, положить рядом.

### Иллюстрации в описании товара
Идущие подряд картинки в блоках описания собираются в одну группу и выводятся
**по три в ряд** на десктопе и по две на телефоне (`groupImages` в
`src/components/ProductContentBlocks.tsx`, ячейка `aspect-4/3`, `object-contain`).
Раньше каждая шла отдельной строкой во всю ширину колонки и растягивалась.
Одиночную иллюстрацию в сетке из трёх не ставим — она выглядела бы огрызком
рядом с двумя пустыми колонками, поэтому показываем её отдельно и не шире 420px.

Групп подряд идущих картинок всего 42: у 35 товаров это одна картинка,
у остальных от двух до восьми.

### Сайт больше не зависит от invit.by
Перенесено **всё**, что раньше тянулось с сайта клиента. Проверка: обход десяти
страниц на двух разрешениях даёт **ноль сетевых запросов к invit.by**.

| Что | Куда | Сколько |
|---|---|---|
| Фото товаров | `src/assets/products` | 208 файлов, 2,8 МБ |
| Иллюстрации в описаниях | `src/assets/content` | 257 файлов, 4,0 МБ |
| Сканы документов | `src/assets/certificates` | 10 файлов, 544 КБ |
| Баннеры разделов | `src/assets/categories` | 2 файла, 88 КБ |
| Фоны слайдера | `src/assets/hero` | 4 файла, 1,1 МБ |

Все приведены к WebP. Иллюстрации описаний — ширина до 900px, quality 80,
в среднем 14 КБ вместо 40-60 КБ на invit.by.

Как подключено:
- `productImage()`, `categoryBanner()`, `certificateImage()` — в `productImages.ts`;
- `contentImage()` и `productGallery()` — в **отдельном** `contentImages.ts`
  вместе с картой `contentImageMap.ts` на 257 адресов;
- пять одиночных картинок (коллажи на главных, баннер обращения) подключены
  прямыми импортами: карта им не нужна.

**Почему карта в отдельном модуле.** Она весит 30 КБ, плюс ссылки на 255 файлов.
Если держать всё в `productImages.ts`, который импортирует карточка товара
в сетке, это уезжает в основной бандл: он вырастал с 281 до 331 КБ в gzip.
Поэтому карта живёт отдельно, а **страница товара грузится через `React.lazy`** —
теперь она отдельный чанк на 53 КБ, а основной бандл вернулся к 278 КБ.

**При обновлении каталога** пересобирать надо и файлы, и карту: скачать адреса
из `PRODUCT_CONTENT`, пережать в WebP, сгенерировать `contentImageMap.ts` заново.

Картинки новостей не переносились: это клипарт нулевых, он удалён из вёрстки
(см. раздел про новости).

### Баннеры разделов
Лежат в `src/assets/categories/`, подключаются через `categoryBanner()` там же,
в `productImages.ts`. Собраны локально в **1280x352** — это двойной размер бокса
(640x176), потому что картинок такой ширины на invit.by нет вовсе:

- **Материалы для монтажа окон** — кадр вырезан из фото героя `tape-application`;
- **Комплектующие для вентиляции** — сцены для этого раздела у клиента не
  существует, поэтому кадр собран из товарного снимка фланцевого профиля на
  подложке `#EEF1F6`. Товар поставлен в своём разрешении, не растянут.

Заливка на карточке сменена с «снизу вверх» на «слева направо»: текст лежит на
тёмном, фото справа остаётся чистым. Счётчик позиций был `text-ink/40` поверх
тёмной заливки, то есть невидим, — стал `text-white/75`.

Коэффициент растяжения баннеров теперь 0.5 на десктопе и 0.26 на телефоне.

Описания: у **54 позиций** описания нет и на самом invit.by (это сопутствующие
товары, не ленты). Придумывать их нельзя, поэтому в карточке пустой абзац просто
не выводится. У 152 позиций краткое описание дословно повторялось первым блоком
подробного — страница говорила одно и то же дважды. Повтор убирает
`dedupeContentBlocks` в `src/lib/product.ts`, заодно снимая заголовок, который
после этого остался бы пустым (блоков стало 1316 вместо 1553).

В названиях 17 позиций сидели HTML-сущности (`&quot;`, `&amp;`) — раскодированы
прямо в `catalogData.ts`.

## Иконки разделов
Карта «слаг подраздела -> иконка» лежит в `src/lib/sectionIcons.tsx`.

**Сейчас на сайте: плоские силуэты** — 13 знаков клиента плюс 6 из MDI, все
одноцветные векторы.

Знаки клиента приходили растром со светотенью, и рядом с векторными запасными
смотрелись как другой набор. Поэтому их **уплощают**: альфа-канал -> potrace ->
сплошная заливка (`scripts/sections/flatten.py`, результат в
`src/lib/sectionIconPaths.ts`, +9KB gzip). Форма предмета остаётся, объём уходит.

Что это дало кроме единства: знак стал вектором и красится `currentColor` — у
выбранного пункта каталога иконка снова красная, с растром так не получалось.

Из MDI взяты шесть: резиновый уплотнитель, две категории и раздел меню
«Ленты EUROBAND» (их в архиве клиента нет), а также кровельные уплотнители и
оснащение воздуховодов — после уплощения крыша становилась похожа на шляпу, а
короб воздуховода на бесформенное пятно.

Исходные webp остались в `src/assets/sections/`: из них пересобираются плоские
знаки, в сборку сами файлы больше не попадают.

Набор менялся трижды, порядок был такой:
1. линейные lucide по мотивам tbmmarket.by;
2. предметные MDI — клиент попросил «как на invit.by»;
3. векторизованный спрайт invit.by — «это sprite и из него надо сделать иконки»;
4. готовые файлы клиента из присланного архива;
5. единый контурный lucide — клиент показал tbmmarket.by и попросил такой же
   стиль (их файлы взять нельзя, это графика ТБМ);
6. **обратно знаки клиента** — «пусть будут старые тоже, но в едином стиле».

Что из этого стоит помнить: у знаков клиента залитые объёмные силуэты, у
lucide тонкий контур, и вместе они не смотрятся. Поэтому запасные знаки берём
из MDI **залитыми** и в том же тёмно-сером `#47474a`.

Цена решения: растр перекрасить нельзя, поэтому у выбранного пункта каталога
знак не подсвечивается — красным помечен только текст.

Скачанные для сравнения знаки ТБМ лежат вне проекта (`~/Downloads/tbm-icons/`)
как справочный материал по стилю: ставить их на сайт нельзя.

Набор менялся дважды. Сначала клиент прислал **tbmmarket.by** — линейные
пиктограммы, взяли lucide. Потом прислал спрайт со **своего invit.by** со
словами «подбери такие»: там не абстрактные знаки, а сами предметы — рулон,
баллон пены, туба герметика, саморез, гайка, вентилятор, и все с заливкой.

Потом клиент сказал прямо: «это sprite и из него надо сделать иконки». Знаки
векторизовали из спрайта. А следом он прислал архив `png_icons_separately.zip`
с теми же знаками начисто: прозрачный фон, разрешение 50-112px. **Сейчас на
сайте стоят они** — `src/assets/sections/*.webp`, приведены к квадрату 192px
(у оригиналов разные пропорции, а в сетке знаки должны быть одного кегля).

Знаки клиента закрывают пятнадцать подразделов из шестнадцати:
- «Герметики, клея, химия, смазки» — по его просьбе стоит знак
  `08-antikorrozionnyy-sprey` из того же архива;
- «Уплотнитель резиновый D, P, E» — единственный без своего знака. Вариант из
  спрайта клиент забраковал: овал в мелком размере читался как ещё один рулон
  и сливался с ПСУЛ. Там **шина** (`mdiTire`) — резина узнаётся, с лентами не
  путается.

Раз спрайтовые векторы больше нигде не показываются, `sectionIconPaths.ts`
удалён. Скрипты в `scripts/sprite/` оставлены: пригодятся, если появится
раздел без готового файла.

**Похожесть знаков — известное место.** «Монтажные ленты» и «ПСУЛ» в архиве
клиента отличаются только хвостом рулона; в 18px в дереве каталога они
различаются с трудом. Это его собственные знаки, поэтому не трогаем.

Цвет у всех знаков одинаковый тёмно-серый `#47474a`: растр перекрасить нельзя,
а разнобой «часть синие, часть серые» был бы заметнее, чем отсутствие
подсветки у выбранного пункта. Выбранный пункт помечен красным текстом.

Ниже — как делали векторизацию, она нужна для этих двух знаков и на случай,
если клиент пришлёт новый спрайт.

Спрайт `catalog/view/theme/meta/img/sprite.png` — растр 25x425: семнадцать
знаков по 25x25 (шестнадцать разделов плюс антикоррозийный спрей, которого в
нашем каталоге нет). В таком виде на плитке 48px он давал бы кашу, поэтому
ячейку увеличивают в 20 раз, размывают (иначе potrace обводит «лесенку»
пикселей) и трассируют **в два тоновых слоя**: тёмный корпус и светлый торец.
Объём знака сохраняется, масштаб — любой.

Соответствие «ячейка -> раздел» не угадывали, а взяли из их же CSS: класс
`.icoNN` со сдвигом фона, где `NN - 61` = номер ячейки; названия — из разметки
главной. Скрипты и инструкция лежат в `scripts/sprite/`, результат — в
`src/lib/sectionIconPaths.ts` (генерируется, руками не править, +5KB gzip).

Material Design Icons (`@mdi/js`) остались только у двух категорий и у раздела
меню «Ленты EUROBAND»: своих знаков для них в спрайте нет.

**Проверять знак в обоих размерах.** На плитке 48px читаемо всё; узкое место —
18px в дереве каталога.

Промежуточный этап (подбор похожих знаков из MDI вместо клиентских) отменён
целиком. Он остался только в истории: коммиты «Иконки разделов — предметные,
в духе invit.by» и следующий за ним.

### Размер и толщина линии
`SectionIcon` принимает `size` — это сетка, в которой рисуется знак (атрибуты
`width`/`height` у svg). **На резкость size не влияет**: SVG векторный и чёток
на любом размере, растра тут нет вообще. Влияет он на другое — толщина линии
задаётся в единицах viewBox 24 и масштабируется вместе со знаком, поэтому
`strokeWidth={1.5}` на 48px даёт 3px реальной линии, и знак выглядит жирным.

Поэтому у крупных (от 32px) включён `absoluteStrokeWidth` — lucide пересчитает
штрих так, чтобы он остался ~1.75px при любом размере. Мелкие (16-20px)
оставлены как есть: там абсолютный штрих, наоборот, жирнит.

**`size` и CSS-размер должны совпадать.** CSS перебивает атрибуты, и знак,
которому задали `size={32}` в классе `w-6` (24px), рисуется линией 1.31px
вместо 1.75px. Проверять аудитом: у всех иконок разделов физическая линия
должна быть 1.75px у крупных и 1.0-1.3px у мелких.

Иконки стоят везде, где перечислены разделы:
- плитки категорий на главной — 48px, линия 1.75px;
- заголовки подразделов в мега-меню — 28px в рамке 40px, линия 1.75px;
- список разделов слева в мега-меню — 20px, у выбранного красная;
- левое дерево каталога — 20px у категорий, 18px у подразделов;
- метка раздела в карточке товара — 16px рядом с названием.

Два подводных камня этого списка:
- у категории «Материалы для монтажа окон» стояла `Layers` — та же иконка, что
  у подраздела «Уплотнительные ленты ПЭС». В одном столбце читалось как повтор,
  поэтому у категории теперь `AppWindow`;
- иконка съедает ~30px ширины, и в 272px-колонке каталога названия категорий
  начали переноситься. Отступы поджаты (`gap-2.5`, `px-2.5`) — переносы остались
  только у названий, которые не помещаются в строку и без иконки.

Раздел мега-меню «Ленты EUROBAND» в каталоге не существует, он собирается из
`TAPE_SUBCATEGORIES`. Его иконка (`Ribbon`) лежит в `BY_CATEGORY` под ключом
`tapes` — это `id` секции меню, а не слаг категории.

До этого пробовали фото первого товара в роли иконки — работало, но клиент
хотел именно пиктограммы.

«Уплотнитель резиновый Д/П/Е» по просьбе клиента переехал со `Spline`
(кривая Безье, к товару отношения не имеет) на `Spool` — катушку.

**SVG руками не рисуем.** Если понадобится знак, которого нет в lucide, лучше
подключить второй набор, чем чертить путь: получится разная толщина и посадка.

## Каталог как обычный магазин
`/catalog` и `/catalog/:categorySlug` обслуживает **одна** страница
(`src/pages/CatalogPage.tsx`), `CategoryPage.tsx` удалён — это были два
компонента с почти одинаковой разметкой.

Формат по референсу **labzerde.com/shop**, который выбрал клиент:
- слева дерево категорий со счётчиками, подразделы раскрываются у выбранной;
- сверху поиск по названию и разделу, запрос живёт в адресе (`?q=`);
- справа сетка карточек и «Показать ещё» по 24 позиции;
- выбранный пункт красный, как в мега-меню.

Верхний блок с двумя большими карточками разделов убран: клиент назвал его
лишним, он дублировал левое дерево.

### Карточка товара
Обычного магазинного вида: фото, раздел мелким синим капсом с линейной
пиктограммой, заголовок, короткое описание, «Подробнее» и «В смету». Таблица
из двух характеристик убрана — она делала карточки разной высоты и повторяла
страницу товара.

### Сколько карточек в ряду
`ProductGrid` принимает `columns` (3 / 4 / 6), классы сеток перечислены
целиком — Tailwind не собирает имена по частям.

Клиент попросил шесть в ряд, посмотрел и решил так: **везде обратно** —
в каталоге три (карточка 315px, влезает полное описание и подпись «В смету»),
на главной четыре и восемь позиций, как было. Режим `columns={6}` в коде
остался, но сейчас нигде не используется: если понадобится плотная витрина,
он готов.

Шесть в ряд у клиента прижилось в другом месте — плитки подразделов
«Комплектующие для вентиляции» на главной (их ровно шесть, встают одной
строкой с 1024px). См. `columnsFor` в `CategoryTiles.tsx`.

При `columns === 6` карточка уходит в режим `compact` (~150-200px шириной):
фото `h-32`, заголовок 13px, описание в две строки, у кнопки сметы остаётся
только знак «+», а бейдж «Собственное производство» заменяется коротким
`EUROBAND` — полный текст рвался на две строки и закрывал фото.

Две ловушки, на которые напоролись именно на узкой карточке:
- **`block` ломает `line-clamp`.** У заголовка стояли и `block`, и
  `line-clamp-2`; `block` перебивал `display:-webkit-box`, обрезка не
  работала, и в 137px-карточке заголовок уходил в семь строк. Убрали `block`.
- **`min-h-11` на заголовке снимать нельзя.** Без него на 375px появлялось
  24 цели меньше 44px. `min-height` обрезке не мешает, в отличие от `display`.

На главной показываем 8 лент собственного производства (`slice(0, 8)` в
`HomePage.tsx`). Пока пробовали шесть в ряд, их было 12 — восемь при шести
в ряд давали ряд и обрубок из двух.

## Корзина (бывшая «смета КП»)
Клиент попросил обычную корзину вместо сметы: «вместо В смету сделать в
корзинку, и чтоб можно было добавить несколько товаров».

Что изменилось:
- подписи везде: «В корзину» / «В корзине», модалка — «Корзина», плавающая
  кнопка и шапка тоже;
- **количество**. На странице товара появился блок «Количество» с −/+ и полем,
  `addToQuote(product, variant, qty)` кладёт сразу столько, сколько выбрали.
  В корзине у каждой позиции те же −/+, внизу «Всего N шт.» и «Очистить»;
- стартовое количество — 1. Было 10 (кто-то заложил метраж рулона), из-за чего
  одна кнопка добавляла сразу десяток;
- **добавление ничего не открывает.** Раньше `addToQuote` сразу показывал
  модалку с формой заказа — на каждое нажатие «В корзину» посреди каталога.
  Теперь кладём молча, обратная связь — счётчик у иконки и подпись «В корзине»
  на кнопке. Форма заказа осталась внутри корзины, её открывает покупатель;
- **иконка корзины в шапке** (`CartButton` в `home-v2/Chrome.tsx`): красный
  бейдж со числом позиций, 44x44, видна и на мобильном — рядом с бургером;
- модалка переписана в синюю палитру `inv-*`. В ней оставались `brand-*`,
  `emerald-600` на галочке и фото товара по прямой ссылке на invit.by —
  единственное место на сайте, где ещё жил внешний хотлинк;
- убрано обещание «за 15 минут» из кнопки отправки: сроков клиент не давал.

Ещё один `emerald-600` нашёлся на кнопке «Добавлено» на странице товара —
заменён на `brand-navy`. Оба зелёных пережили прошлый аудит по той же причине:
Tailwind 4 отдаёт emerald как `oklch`, а скрипт искал `rgb`.

### Корзина — отдельная страница `/cart`
Модалка удалена (`Modals/QuoteCartModal.tsx`), состояния «открыта/закрыта» в
контексте больше нет. Заказ из нескольких позиций в окне поверх каталога
собирать неудобно: не видно, что уже набрано.

Страница: слева позиции с ±, справа липкая карточка «Оформить заявку» со
счётчиками, телефоном и комментарием. Пустое состояние ведёт в каталог.

Фото позиции 96px (80px на мобильном): по 64px ленту от ленты не отличить, а
именно по виду рулона покупатель и опознаёт товар.

«Очистить корзину» — кнопка с рамкой и корзиной-иконкой, **в два шага**:
нажатие показывает «Удалить все позиции? Да, удалить / Отмена». Набранный
заказ не должен сноситься одним случайным нажатием, а отмены у нас нет.

**Корзина переживает перезагрузку** (`localStorage`, ключ `invit:cart:v1`).
Пока корзина была модалкой, это было не нужно — состояние жило в памяти SPA.
Как только она стала страницей, на неё стали заходить по прямой ссылке и
обновлять её, и заказ терялся. В хранилище кладём только `id`, типоразмер и
количество; товар достаём из каталога, иначе туда уедут описания и картинки.
Позиции, которых больше нет в каталоге, при чтении отбрасываются.

### Вариативность размеров убрана
Была: выбор типоразмера на странице товара и в быстром просмотре, счётчики у
каждого размера, ключ корзины `id :: типоразмер` (одна лента в трёх ширинах =
три строки). Клиент попросил убрать — размеры покупатель пишет в комментарии
к заявке.

Что стало: у товара только количество, ключ корзины — просто `id` товара, поле
`selectedWidth` из модели и хранилища удалено. Старые корзины в браузерах
покупателей не ломаются: в сохранённых данных рядом с `id` лежала ширина, при
чтении она просто игнорируется.

`variantOptions` в `lib/product.ts` остался — размерные ряды по-прежнему
показываются в характеристиках товара, там это описание, а не выбор.

Внутренние имена (`quoteCart`, `addToQuote`) остались от сметы: переименование
дало бы большой дифф и ничего больше.

## Поле Email в формах заявок
Добавлено необязательное поле Email рядом с телефоном во все три формы сайта:
- корзина `/cart` («Оформить заявку» — `CartPage.tsx`);
- форма запроса расчёта `RequestForm.tsx` (общая для главной варианта 2,
  страницы контактов и, после следующего пункта, главной варианта 1);
- модалка обратного звонка `CallbackModal.tsx`.

Бэкенда по-прежнему нет — поле просто хранится в состоянии формы, как и
остальные. Телефон остаётся единственным обязательным контактом.

## Форма запроса на главной варианта 1 (`ContactBanner`)
Правая колонка блока контактов на живой главной (`/`) была тёмной карточкой
с двумя кнопками-ссылками («Связаться с нами», «Документы») — без единого
поля ввода. Клиент попросил вернуть форму.

Заменена на ту же `RequestForm`, что уже стоит на `/contacts` и `/v2`,
в белой карточке с рамкой (`bg-white border border-inv-border rounded-[8px]`).
Токены `inv-*` на этой странице уже доступны: `LayoutV2` оборачивает все
маршруты классом `theme-v2`, включая `/`, так что форма красится в те же
цвета без дублирования стилей.

Следом клиент попросил вернуть и фоновое фото под этим блоком (в самой
первой версии сайта тут было широкое фото на всю ширину). Добавлено фото
`hero/tape-application.webp` (рука с рулоном EUROBAND, единственное из
семи фото в `hero/`, ещё не занятое слайдами) с тем же градиентом
затемнения, что в `Hero.tsx`: тёмно слева под текстом, светлее к центру,
где стоит непрозрачная белая карточка формы — сама карточка не зависит от
фона и одинаково читается в любом месте градиента. На мобильном (стек в
одну колонку) добавлен второй, вертикальный градиент — иначе текст поверх
фото нечитаем на всю ширину экрана.

Проверено на мобильном (375px, скриншот — фото видно, текст читается,
карточка формы аккуратно перекрывает низ фото) и через DOM на десктопе
(картинка загружена 1920px, без горизонтального переполнения, форма
умещается в колонку) — устойчивый скриншот десктопа в этой сессии не
снялся из-за сбоя захвата кадра при глубокой прокрутке панели
предпросмотра, воспроизводится независимо от содержимого страницы.

## Кнопка «Связаться с нами» в блоке «Для нас важны:»
После переезда формы в `ContactBanner` кнопки «Связаться с нами» и
«Документы» из его тёмной карточки исчезли — они там были до формы.
Клиент попросил вернуть кнопку связи, но уже в соседний блок
«Мы работаем с 2001 года» (`SinceBlock.tsx`), в его тёмную карточку
«Для нас важны:», после последнего абзаца. Ссылка на `/contacts`,
стиль — сплошная синяя кнопка (`bg-brand-blue`), как в остальных CTA
на светлых/тёмных подложках сайта. Проверено на мобильном скриншотом,
на десктопе — через DOM (кнопка внутри карточки, без переполнения).

Следом клиент попросил, чтобы клик открывал попап с формой, а не вёл на
страницу контактов. Кнопка стала `<button onClick>`, дёргает тот же
`shop.openCallback(note)`, что и остальные CTA на сайте (шапка, hero,
плавающая кнопка) — открывает `CallbackModal` с полями имя/телефон/email/
комментарий, комментарий предзаполнен пометкой источника клика. `SinceBlock`
принял проп `onOpenCallback`, прокидывается из `HomePage.tsx`.

## Импорт 594 товаров крепежа из прайса поставщика
Клиент прислал полную выгрузку прайса поставщика (CSV, 9022 позиции: крепёж,
ручной инструмент, СИЗ, измерительный инструмент, стройматериалы, вентиляция,
хозтовары) и архив фото (1340 файлов). Прежде чем что-то добавлять, сверил
масштаб: фото по внутреннему `ID` из CSV совпало только у 594 позиций — 746
фото в архиве относятся к товарам, которых в этой выгрузке уже нет (более
старый архив фото). Показал клиенту разбивку по категориям и уточнил объём —
подтвердили «все, у чего есть фото», то есть эти самые 594.

**Состав 594 позиций** (после сверки почти всё — крепёж бренда STARFIX):
саморезы и шурупы 263, дюбельная техника 203, остальной спецкрепёж (заклёпки,
скобяные изделия, перфорированный крепёж, хомуты, анкерный крепёж) 107,
инструмент и СИЗ (перчатки, малярный/слесарный инструмент) 17, клей 3,
траверса монтажная для вентиляции 1.

**Распределение по каталогу** — уместилось в существующую структуру, без
новых разделов и без правки `division`:
- Саморезы/дюбели — две новые подкатегории под «Материалы для монтажа окон»:
  `samorezy-i-shurupy` (263), `dyubelnaya-tehnika` (203). Иконки — `mdiScrewLag`
  и `mdiPin` из MDI (в архиве клиента и так знака нет).
- Остальной спецкрепёж (107) — в существующую «Крепёж для окон, кровли,
  фасадов»: 26 → 133.
- Инструмент/СИЗ (17) — в «Инструмент, СИЗы»: 54 → 71.
- Клей (3) — в «Герметики, клея, химия, смазки»: 35 → 38.
- Траверса (1) — в раздел вентиляции, «Профиль монтажный – траверса»: 4 → 5.

**Данные и фото.** Скрипт (`/tmp/psul_import/gen.py`, разовый, в репозиторий
не входит) читает CSV+zip и генерирует `src/data/importedFasteners.ts`
(`IMPORTED_FASTENERS: Product[]`, 594 записей) — импортируется в
`catalogData.ts` и добавляется в конец `PRODUCTS` через spread, чтобы не
трогать существующий литерал на 5000+ строк. Фото приведены к тому же виду,
что и остальной каталог: паддинг до квадрата на белом, 500×500, WebP quality
82 — лежат в `src/assets/products/` рядом с остальными 208.

Slug/id — транслитерация названия + `_ID` поставщика для гарантии
уникальности (собственных читаемых slug'ов, как у товаров с invit.by, тут
взять неоткуда — исходник это строка прайс-листа, не карточка сайта).
`description` — не выдумывал маркетинговый текст (source data его не
содержит): шаблонная фраза «Прямая поставка от поставщика. Актуальный
остаток, объём и цену уточняйте у менеджера» — соответствует принятому на
сайте подходу «без цен, считает менеджер». В `specs` — реальные поля из
прайса: артикул поставщика, страна производства, кратность заказа (когда
не 1). `sourceUrl` пустой — у этих позиций нет исходной страницы на сайте
(поле нигде не рендерится, только служебное).

**Проверено:** `tsc --noEmit` чисто; все 594 картинки на диске (0 пропusков);
0 дублей id; 0 пересечений id с существующими 208 товарами. На dev-сервере:
каталог показывает 802 (было 208), обе новые подкатегории открываются с
верным числом позиций (263 и 203), карточка товара из выборки (в т.ч.
случайная, не только первая) рендерится с фото и специфика­циями, поиск
по «дюбель» находит новые товары, мегаменю в шапке показывает новые
подкатегории с счётчиками. Консоль без ошибок (кроме служебного HMR-сокета
dev-сервера).

**Побочный эффект — стоит знать.** `catalogData.ts` целиком уходит в
основной JS-бандл (грузится на каждой странице, не только в каталоге) —
так было и раньше, но с 594 новыми товарами основной бандл вырос
с 302 КБ до 387 КБ gzip (+28%). Полноценно вынести каталог в отдельный
ленивый чанк — отдельная задача (сейчас так уже сделан `productContent.ts`
и `contentImages.ts`, но `PRODUCTS`/`CATEGORIES` используются синхронно
в шапке, на главной и т.д. — потребует более широкой правки). Не делал
это в рамках этой задачи, клиент не просил — если размер бандла станет
проблемой, можно вернуться к этому отдельно.

## Настоящие фото ленты ПСУЛ от клиента
Клиент прислал 6 фото своей продукции — рулоны ПСУЛ EUROBAND (с этикеткой,
россыпью, в разрезе, на затемнённом фоне «ступеньками»). Разошлись по трём
местам:

- **Карточка товара `psul-euroband-dlja-okon`.** Старое фото ролика (500×500,
  тянулось с invit.by через `productImages.ts`) заменено на присланное —
  тот же ракурс (рулон разматывается в полосу), но чётче и без сжатия
  invit.by. Файл просто перезаписан по тому же пути, код не менялся:
  `productImage()` находит локальный файл по id товара.
- **Галерея того же товара.** В `productContent.ts` добавлены ещё два фото
  (этикетка «ПСУЛ EUROBAND» с роликами и россыпь из 4 рулонов) — они не
  из invit.by, поэтому `CONTENT_IMAGE_MAP` (обычно генерируется скриптом
  по адресам invit.by) пришлось дополнить руками, синтетическими ключами
  вида `local:psul-euroband-etiketka`.
- **Шестой слайд hero.** У ПСУЛ — одной из трёх флагманских лент
  собственного производства — своего слайда не было, хотя окна, сэндвич,
  ПЭС и демпферная лента уже у себя есть. Добавлен `src/assets/hero/
  psul-tape.webp`: то самое фото «ступеньками» на затемнённом фоне, тот же
  приём градиента, что у остальных пяти слайдов. Клиент отметил, что тёмные
  кадры можно осветлить — прогнал через Pillow (`ImageEnhance`, +18%
  яркости, +6% контраста) перед тем, как класть в проект.

Из 6 присланных фото использовано 5. Россыпь роликов под углом пошла
седьмым кадром в галерею **второго** товара ПСУЛ — `psul-dlja-brusa`
(«для деревянного домостроения»): в его галерее было 6 фото, и все —
монтажные схемы и примеры на срубе, ни одного простого фото самой ленты.
Второй ракурс «ступенек» (мягкий фокус) не пригодился — это тот же кадр,
что уже стоит в hero, только чуть иначе снятый, отдельного места под
почти-дубль не нашёл. По просьбе клиента оставлен про запас, тем же
способом осветлён и лежит рядом с остальными: `src/assets/hero/
psul-tape-alt.webp`, нигде не подключён (как и неиспользованный
`roof-profile-sheets.webp` рядом) — под рукой, если слайд захотят
поменять на этот кадр.

Проверено скриншотами: слайд на десктопе и мобильном (375px), галерея
товара — все миниатюры кликабельны и показывают нужные фото.

## Hero: не все баннеры успевали загрузиться
Клиент сообщил, что часть баннеров в hero не показывается. Причина —
все 6 фоновых фото слайдов рендерились сразу (`SLIDES.map`, без ленивой
загрузки), это больше 1.5 МБ одним залпом на первом экране. Пока грузились
первые и самые тяжёлые (`floor-damper-tape.webp` 417 КБ,
`roof-standing-seam.webp` 323 КБ), карусель через 7 секунд уже листала
на 5-й и 6-й слайд — а там ещё пусто, только тёмный фон под градиентом.
Раньше при пяти слайдах это тоже было проблемой, просто с шестым (ПСУЛ)
общий вес вырос и стало заметнее.

Исправлено в `Hero.tsx`: вместо рендера всех `<img>` сразу — набор
«загруженных» индексов, стартует с `{0, 1}` (текущий и следующий). При
смене активного слайда в набор добавляется новый текущий и его следующий
сосед; раз добавленное не убирается — иначе кросс-фейду не из чего будет
проигрывать возврат на предыдущий слайд. У каждого слайда так есть все
7 секунд показа предыдущего, чтобы скачаться, а на первом экране грузятся
только 2 фото вместо 6.

Проверено: на чистой загрузке в DOM только 2 фоновых `<img>` (текущий и
следующий), через ~8 секунд (после пары автопрокруток) — уже все 6,
все `complete: true`. Файлы отдаются 200 OK, дело было именно в порядке
и объёме загрузки, не в битых путях.

**Следом отдельно сжал сами файлы пожёстче.** Пять самых тяжёлых фото
в `src/assets/hero/` (`floor-damper-tape`, `roof-standing-seam`,
`tape-slab-joint`, `psul-tape`, `tape-application`) были на WebP quality
~80-85 — пережал через Pillow на quality 48, method 6. Не в один заход
поверх старых: сперва вернул оригиналы из git (`git checkout HEAD --`),
иначе второй проход лосси-сжатия поверх первого портит картинку сильнее,
чем даёт экономии. Итог: 1,7 МБ → 0,99 МБ на пять файлов (-42%), на глаз
разницы нет — фон под градиентом и мелкий шум в WebP не считываются
даже вблизи (сверял на несжатом кадре с лентой EUROBAND — текст и фактура
ковролина не потеряли резкость).

## Логотип в подвале — белый вариант
Клиент прислал белую версию знака (`src/assets/logo/invit-light.svg`): текст
белый, сам знак фирменный синий. Она и стоит в подвале — без плашки и отступов.

До этого там был цветной вариант на белой подложке: тёмно-синий текст знака
тонул в фоне подвала, и плашка была единственным способом его показать. С белой
версией подложка не нужна.

В шапке остаётся цветной `invit-color.svg` — она на белом фоне.

## Знак «Лучший строительный продукт года 2013»
Закреплён у правого края, по центру по вертикали (`AwardBadge` в
`home-v2/Chrome.tsx`): 96px на широком экране, 80px с 768, 56px на телефоне.

Отдельного файла со знаком у клиента нет: он вшит в слайд
`slides/slide-invit-euroband-1092x337.jpg`. Вырезали скриптом
`scripts/award/extract.py` — знак ищется по красным пикселям в верхней полосе
кадра (ниже фото ленты, там тоже есть красноватые точки), круговая маска,
увеличение втрое и лёгкая резкость. Маска на 3.5% уже кадра: по краю печати
оставалась полоска фона слайда, на тёмной подложке она читалась серым ободком.

Знак **некликабельный** (`pointer-events-none`): самого диплома среди
документов на сайте нет, и клик уводил бы в раздел, где его не найти.
Если клиент пришлёт скан — можно повесить ссылку на `/certificates`.

`pointer-events-none` заодно решает главную проблему знака на телефоне: он
лежит поверх содержимого, но нажатия проходят сквозь него — проверено, под
центром знака кликается то, что под ним, а не сам знак.

## Анонсы новостей — 250 знаков
Карточка новости показывает первый абзац `content`, подрезанный до 250 знаков
по границе слова, дальше многоточие (`excerpt` в `NewsPage.tsx` и
`home/NewsGrid.tsx`). `summary` из выгрузки не годится — он обрезан на
полуслове ещё в источнике.

`line-clamp` снят в обоих местах: длину задаёт обрезка по знакам, а клэмп
резал бы текст второй раз, уже по строкам, и до 250 знаков дело бы не дошло.
Карточки на главной от этого выросли примерно на 40px и остались одной высоты
(`items-stretch` в сетке).

## Поиск в шапке и меню
В основное меню добавлена «Главная», в подвал — «Политика конфиденциальности».

**Поиск** (`HeaderSearch` в `home-v2/Chrome.tsx`) — кнопка-лупа, по клику
открывается попап: карточка 640px под шапкой, поле высотой 56px с автофокусом,
кнопка «Найти в каталоге», чипы «Часто ищут» и подсказка про Esc. Появляется
затемнение с размытием, карточка выезжает сверху за 240 мс; при
`prefers-reduced-motion` — сразу. Прокрутка фона на время попапа блокируется.

Чипы: ПСУЛ, ПЭС, Герметики, Крепёж, Пена, Уголки — каждый проверен по каталогу,
пустых нет (5, 8, 41, 26, 18 и 6 совпадений).

Своей выдачи попап не делает — уводит в каталог с готовым запросом
(`/catalog?q=…`), где поиск и фильтр по разделам уже есть.

Полем прямо в строке шапки это было первой версией, но вместе с пятым пунктом
меню оно не помещалось: при окне 1024 шапка распирала документ до 1117px, при
1536 — до 1555px. Кнопка занимает 44px и помещается везде.

**Брейкпоинты шапки.** Пункты меню и правый блок показываются с `xl` (до
«Главной» в меню хватало `lg`), телефон — с `2xl`. До `xl` меню живёт под
бургером, а лупа и корзина остаются в строке. Проверено на
1024/1280/1440/1536/1920: переполнения нет.

## Поиск: ранжирование и транслитерация брендов
Клиент прислал две конкретные жалобы: запрос «очиститель» выдаёт вперемешку
пену, сам очиститель «ещё нужно поискать»; запрос «космофен» не находит
ничего. Обе — из одного корня: старый поиск (`CatalogPage.tsx`) был плоским
`includes()` по `title + shortTitle + subcategoryName` без какого-либо
ранжирования.

**Разобрал «очиститель» по данным.** 23 совпадения, из них только 8 — товары,
где слово реально есть в названии; остальные 15 — это вся пена подкатегории
«Пена монтажная, **очиститель** для пены» (слово только в имени подраздела,
не в товаре). Без ранжирования оба типа совпадений весили одинаково и шли
вперемешку в порядке каталога.

**«Космофен» не находился по другой причине:** бренд в данных написан
латиницей — `COSMOFEN`. Обычный `includes()` кириллицу с латиницей не
сопоставляет вообще, только точную строку.

**Правки в новом `src/lib/search.ts`** (`searchProducts`, подключён в
`CatalogPage.tsx` вместо старого фильтра):
- Ранжирование по весу: начало заголовка/краткого заголовка — 3,
  совпадение где-то в заголовке — 2, совпадение только в названии
  подкатегории — 1. Результат сортируется по весу, а не остаётся в
  порядке каталога.
- Кириллические алиасы брендов (`старфикс` → `starfix`, `космофен` →
  `cosmofen`, `евробанд` → `euroband` и ещё 13 — все латинские бренды,
  реально встречающиеся в каталоге, включая только что добавленный
  STARFIX/STARTUL) плюс общая транслитерация запроса как запасной
  вариант для остального. `кс` на конце слова заменяется на `x`
  (старфикс → starfiks → starfix, а не мимо).

Проверено: «очиститель» — все 8 настоящих очистителей в начале списка,
пена — после них (было вперемешку). «Космофен» — 0 → 5 найденных
(диффузионный клей, 3 очистителя серии, секундный клей). «Старфикс» —
570 (весь новый крепёж). «Евробанд» — 12, свои ленты. Пустой запрос —
экран «Ничего не нашлось» с подсказками не сломан. Проверено и через
прямой URL `/catalog?q=…`, и через попап поиска в шапке (реальный ввод
в поле). `tsc` чисто, консоль без ошибок.

### Поиск только по названию и артикулу + живая выдача в шапке
Клиент попросил: искать строго по названию и артикулу, а поиск в шапке
сделать «полноценным».

**Подкатегории убраны из области поиска.** Ранжирование (см. выше) убирало
пену из топа, но она всё равно оставалась в выдаче: запрос «очиститель»
давал 23 позиции, из них 15 — пена из раздела «Пена монтажная, очиститель
для пены». Теперь ищем только по `title`, `shortTitle` и `sku` — те же
8 настоящих очистителей и ничего лишнего.

**Артикул стал полем модели.** У 594 товаров из прайса артикул лежал только
внутри `specs` (строка «Артикул поставщика»), для поиска это неудобно —
добавлено поле `sku` в `Product` и проставлено во все 594 записи
`importedFasteners.ts`; в `scripts/import-fasteners/gen.py` тоже, чтобы
пересборка прайса его не потеряла. У товаров с invit.by артикулов нет —
поле опциональное. Вес в поиске: точное совпадение артикула (4) > артикул
содержит запрос (3) > начало названия (2) > название содержит (1).

**Попап в шапке теперь показывает товары.** Раньше он только уводил на
`/catalog?q=…`, своей выдачи не имел. Теперь по мере ввода показывает до
6 совпадений — фото, название, артикул — клик ведёт сразу на товар. Enter
по-прежнему открывает полную выдачу в каталоге. Чипы «Часто ищут» остались,
но видны только при пустом поле и теперь подставляют запрос в поле, а не
уводят со страницы.

### Шапка в две строки
Пункты меню + правый блок (поиск, телефон, корзина, «Запросить расчёт») в
одну строку не помещались — телефон приходилось прятать до `2xl`, а меню
целиком уезжало под бургер уже на `xl`. Клиент попросил развести на две
строки. Итоговая раскладка (после уточнения клиента):

- **Первая строка:** логотип → поле поиска (растягивается, до 560px) →
  телефон. `justify-between`.
- **Вторая строка:** слева «Каталог товаров» (мега-меню), по центру разделы
  сайта (`flex-1 justify-center`), справа корзина и «Запросить расчёт».

Каталог сперва стоял в первой строке рядом с логотипом — клиент попросил
опустить его вниз, к остальным пунктам меню, и развести содержимое обеих
строк по краям.

Порог перехода на бургер опущен с `xl` (1280) до `lg` (1024): в две строки
всё помещается заметно раньше. Проверено на 1024, 1280 и 1440 —
переполнения нет, телефон виден всегда.

### Поиск полем прямо в шапке (`HeaderSearchField`)
Клиент попросил в шапке «полноценный поиск», а не кнопку-лупу. На десктопе
теперь настоящее поле ввода в первой строке; подсказка с товарами
раскрывается прямо под ним (до 6 позиций — фото, название, артикул; внизу
ссылка «Показать все N позиций»). Клик по позиции ведёт на товар, Enter —
в каталог с полной выдачей, Esc и клик мимо закрывают подсказку, не стирая
запрос.

Попап на весь экран (`HeaderSearch`) остался — он используется на
мобильном, где для поля в строке шапки места нет. Логика поиска у обоих
общая (`lib/search.ts`), так что выдача совпадает.

**Про проверку в браузере.** Синтетический ввод (нативный сеттер `value` +
`dispatchEvent`) в этом компоненте не показывает подсказку: React не
успевает дорисовать до синхронного чтения DOM в том же вызове. Проверять
надо настоящими кликом и вводом через инструмент браузера — тогда всё
видно. Отдельная тонкость: у `<input type="search">` есть нативный
крестик очистки, и без него текст в поле при повторных прогонах
накапливается («псулпсулпсул»), из-за чего выдача выглядит пустой.

## Политика конфиденциальности (`/privacy`)
Текст описывает только то, что сайт делает на самом деле — это сверялось по
коду: аналитики, пикселей и рекламных куки нет, единственное хранилище —
корзина в `localStorage`. Ничего про «передаём партнёрам» и сроки хранения,
чего клиент не подтверждал, там нет.

**Текст юридически не согласован.** Перед боевым запуском его должен посмотреть
юрист компании — особенно если появятся аналитика, рассылка или передача
заявок в CRM. Отдельно: сейчас формы никуда не отправляются (обработчик
локальный), при подключении приёма заявок политику надо дополнить.

## Структура страниц
| Маршрут | Страница | Файл |
|---|---|---|
| `/` | Главная (сокращённая) | `src/pages/HomePage.tsx` |
| `/catalog` | Каталог: дерево категорий + поиск + все позиции | `src/pages/CatalogPage.tsx` |
| `/catalog/:categorySlug` | Тот же каталог с выбранным разделом (`?sub=`) | `src/pages/CatalogPage.tsx` |
| `/catalog/:categorySlug/:productSlug` | Карточка товара (slug = артикул в нижнем регистре) | `src/pages/ProductPage.tsx` |
| `/about` | О компании и производстве | `src/pages/AboutPage.tsx` |
| `/certificates` | Сертификаты | `src/pages/CertificatesPage.tsx` |
| `/news`, `/news/:newsId` | Новости и статья | `src/pages/NewsPage.tsx`, `NewsArticlePage.tsx` |
| `/contacts` | Контакты обоих офисов + форма КП | `src/pages/ContactsPage.tsx` |
| `/cart` | Корзина и заявка | `src/pages/CartPage.tsx` |
| `/privacy` | Политика конфиденциальности | `src/pages/PrivacyPage.tsx` |
| `/v2` | Второй вариант главной (палитра American Express) | `src/pages/HomePageV2.tsx` |
| `*` | 404 | `src/pages/NotFoundPage.tsx` |

Адреса собраны в одном месте — `src/routes.ts` (`paths.*`). Общая обвязка (шапка, подвал,
плавающие кнопки, модалки) — `src/components/Layout.tsx`. Смета КП и модалки живут в
`src/context/ShopContext.tsx` над роутером, поэтому корзина не сбрасывается при переходах.

Порядок блоков на главной (`src/pages/HomePage.tsx`), по требованию клиента категории и товары
идут сразу после первого экрана:
герой (слайдер) -> **категории** -> **товары** -> о компании (коллаж) -> «работаем с 2009» ->
новости -> обращение на фото -> сертификаты.
Страницу «Статус заказа» убрали по просьбе клиента вместе с демо-заказами `MOCK_ORDERS`.
Блоки главной лежат в `src/components/home/`.

## Заголовки: не ставить `max-w` уже колонки
На четырёх заголовках стояло ограничение по ширине (`max-w-[20ch]`,
`max-w-[16ch]`), которое было **уже самой колонки**: 445px при колонке 636px.
Оно не улучшало длину строки, а просто рвало заголовок на лишнюю строку.
«Собственное производство в Минске» шло в три строки вместо двух, ещё три
заголовка — в две вместо одной. Ограничения сняты, ширину задаёт колонка.

Заодно смягчена шкала кегля: было `md:text-[40px]`, а на `lg` колонки
становятся вдвое уже (448px), и 40px снова давали лишнюю строку. Стало
`md:text-[32px] xl:text-[40px]`.

Проверяется скриптом: он клонирует заголовок без ограничения и сравнивает
число строк. Если без `max-w` строк меньше — ограничение лишнее.

### Правки по замечаниям клиента (август)
- **«Сопутствующие товары» убраны с главной**: список занимал экран и дублировал
  каталог.
- **Категории вместо подразделов.** Раньше шли плитки подразделов лент, следом
  сетка товаров — получались «товары и товары». Теперь категории каталога,
  а ниже товары.
- **Формат категорий переделан дважды.** Сначала были две крупные карточки
  с описанием и списком подразделов в две колонки — клиент сказал, что читается
  тяжело, и попросил «иконками либо картинками просто». Стало: два блока
  по категориям, внутри плитки подразделов — картинка, название, счётчик,
  без описаний и вложенных списков. Число колонок подбирается по числу плиток
  (10 -> по 5, 6 -> по 3), иначе в последнем ряду остаётся одинокая плитка.
- **Блок обращения** был широким фото с тёмной карточкой поверх. Заменён на ту же
  композицию, что у «Мы работаем с 2009 года»: контакты слева на светлом,
  действие справа на тёмном.
- **Новости** переведены в формат карточек с обложкой (референс клиента).
  Обложки подбираются по теме в `src/lib/newsCovers.ts`: сертификация получает
  скан сертификата, заметка про склад — фото той самой ленты, остальные — кадры
  производства. Настоящих фото к событиям у клиента нет.
- **Подвал**: логотип теперь тот же цветной, что в шапке (был монохромный
  с инверсией), и перечислены все разделы — добавлены «Главная» и «Каталог»,
  которого не было, потому что в шапке он живёт в мега-меню.

## Главная варианта 1 (`/`)
Раскладка осталась своя — этим она и отличается от `/v2`, сравнивать надо
структуру, а не цвета. Поправлено вот что:

- **Почти чёрные блоки.** «Для нас важны», баннер обращения и подложка героя
  были `bg-ink` (#1A1A1A), пока весь остальной сайт тёмно-синий. Переведены
  на `brand-navy`, под темой это #162C58.
- **Огромная выцветшая надпись «работаем с 2009 г»** — приём из середины
  десятых, к тому же дублировала заголовок слева. Вместо неё три числа,
  которые считаются по каталогу: лет на рынке, лент своего производства,
  документов.
- **Сопутствующие товары** были стеной из одиннадцати чипов. Теперь список
  в три колонки со счётчиками, как в каталоге.
- **Новости**: четыре текстовые карточки в ряд выходили узкими и очень
  высокими, стало три.
- **Синий текст на тёмно-синем** («Для нас важны», «Спасибо, что выбрали нас»)
  перекрашен в белый.
- **Точки слайдера** были высотой 6px: полоска осталась тонкой, кликабельная
  область стала 44px.
- **Кадр в коллаже** — обрезанный баннер с впечатанной надписью, слева торчало
  оборванное «AND». Заменён на фото монтажа. Баннер обращения по той же причине
  переведён на своё фото кровли.

## Второй вариант главной (`/v2`)
Альтернатива для показа клиенту. Раскладка и ритм взяты из дизайн-системы
American Express (много воздуха, сдержанная анимация 240 мс на
`cubic-bezier(0.4,0,0.2,1)`), **цвета — клиента**:

| Роль | Цвет | Откуда |
|---|---|---|
| Действия: кнопки, ссылки, акценты | `#3F5B9E` | фон активного пункта меню на invit.by |
| Тёмные поверхности | `#162C58` | тот же синий, затемнён. Белый текст 13.7:1 |
| Марка и выбранный раздел каталога | `#D91920` | логотип EUROBAND |
| Подложки | `#F5F7FA` / `#EEF1F6` | холодные светлые, вместо чёрно-серых плит invit.by |

**Правило двух цветов:** синий отвечает за действия, красный — только за марку
EUROBAND и выбранный пункт каталога. Больше нигде красного нет.
Клиент просил «не мрачно, как на текущем сайте», поэтому тёмных плит по фону нет:
страница светлая, тёмно-синие только герой и полоса документов.

Токены — в `src/index.css` под префиксом `inv-`, основной сайт они не задевают.

### Весь сайт в синем с красным
Все страницы (`/`, каталог, о компании, документация, новости, контакты, 404)
остались теми же компонентами, но переехали под `LayoutV2`
(`src/components/home-v2/LayoutV2.tsx`): шапка с мега-меню и подвал из
варианта 2 плюс класс **`theme-v2`**.

Класс подменяет переменные бренда в своей области видимости (`src/index.css`):
зелёный становится синим, тёмные плиты тёмно-синими. Работает потому, что
утилиты Tailwind 4 ссылаются на переменные (`background-color: var(--color-brand-green)`),
а не подставляют цвет при сборке. Разметку страниц править не пришлось.

Красный в каталоге ровно там же, где в меню: **выбранный подраздел** в боковом
списке и в чипах на телефоне, **выбранный типоразмер** на странице товара.
Кнопка «Добавить в смету» осталась синей: действие всегда синее, иначе два
красных рядом спорят за роль указателя.

Кнопка «Запросить расчёт» в шапке ведёт себя по-разному: на `/v2` это якорь
к форме `#zapros`, на остальных страницах формы нет, поэтому туда передаётся
`onRequest` и открывается модалка обратного звонка.

**Зелёного на сайте не осталось.** Проверяется скриптом, который перебирает все
элементы на всех страницах и ищет цвета с преобладанием зелёного канала.

**Обвязка варианта 1 больше не используется:** `Layout.tsx`, `TopBar.tsx`,
`Header.tsx`, `Footer.tsx`, `MegaMenu.tsx` не импортируются ниоткуда и в сборку
не попадают. Файлы оставлены на случай, если понадобится вернуться; если решено
окончательно — их можно удалить.

Разница между `/` и `/v2` теперь только в раскладке: цвета у обеих главных
одинаковые, сравнивать удобнее.

### Мега-меню (`src/components/home-v2/MegaMenu.tsx`)
Формат выбрал клиент по референсу **stmg.by**: слева список разделов, справа
широкая панель с колонками. Отличие по цветам: выбранный раздел красный
(в референсе оранжевый), заголовки подразделов синие.

Три раздела слева: «Ленты EUROBAND» (5 подразделов лент), «Материалы для монтажа
окон» (10), «Комплектующие для вентиляции» (6). Справа для выбранного раздела
показываются подразделы синими заголовками, под каждым до пяти товаров
(собственное производство первым).

У каждого подраздела слева от заголовка **линейная пиктограмма** раздела.

Панель позиционируется относительно всей шапки, а не кнопки: на обёртке кнопки
`relative` намеренно нет, иначе панель упирается в правый край окна.
На телефоне мега-меню скрыто, разделы каталога уходят в обычное меню списком.

Живёт **вне** общего `Layout`: своя шапка и подвал в `src/components/home-v2/Chrome.tsx`,
секции — в `Sections.tsx`. Иначе зелёная обвязка смешивалась бы с синей палитрой.

Порядок секций (у каждой свой тип раскладки, ни одна не повторяется):
герой-слайдер -> полоса из четырёх фактов -> бенто пяти разделов лент ->
горизонтальная лента 12 товаров -> о компании (коллаж из двух фото) ->
принципы («работаем с 2009 года», закреплённый заголовок и список) ->
тёмная полоса с документами -> список новостей -> контакты и форма запроса.

**Герой** — слайдер из четырёх кадров в скруглённой карточке шириной `95vw`
(радиус 12px). Кадры те же, что в варианте 1. Слайды лежат в одной ячейке грида,
поэтому высота карточки не прыгает; элементам грида нужен `min-w-0`, иначе длинное
слово в заголовке распирает слайд шире карточки на телефоне.

**Анимации на GSAP** (`src/components/home-v2/gsap.ts`), motion остаётся только на
основном сайте — библиотеки не смешиваются в одном дереве. Примитивы лежат в
`Chrome.tsx`, каждый со своей задачей:
- `Fade` — появление одиночного блока при прокрутке, один ScrollTrigger на блок;
- `FadeGroup` — лесенка для сеток и списков: один триггер на всю группу, дети
  помечаются `data-fade-item`. Так анимируются факты, бенто, лента товаров,
  принципы, сканы документов и строки новостей;
- `CountUp` — числа в полосе фактов досчитываются до значения, когда попадают в кадр.
  Ширина зарезервирована через `min-width: Nch`, иначе строка дёргается при счёте;
- `useScrollParallax` — привязка к прокрутке через `scrub`. Стоит на фоне героя (-6%),
  крупной ячейке бенто (-5%) и коллаже о компании (фото -5%, товар +4%, в разные
  стороны). Кадры делаются выше контейнера на 10-12%, иначе параллакс оголит край;
- перекрёстное затухание текста слайдов героя (уходящий 0.16 с, входящий 0.24 с
  с задержкой 0.2 с, иначе два заголовка накладываются).
Всё выключается при `prefers-reduced-motion`. Страница грузится через `React.lazy`:
GSAP и код варианта 2 уезжают в отдельный чанк, основной бандл похудел с 264 до 211 КБ
в gzip.

**Мобильная вёрстка сжата отдельно от десктопа.** Все компактные значения стоят
без префикса, десктопные возвращаются через `sm:` и `lg:`, поэтому широкий экран
не изменился ни на пиксель (проверяется замером: h1 52px, h2 40px, отступ секции
96px, карточка ленты 248px). Страница на 375px стала короче с 8417 до 7074px.
Что ужато: вертикальные отступы секций (`py-10 sm:py-14 lg:py-24`), кегль
заголовков, высоты фото и ячеек бенто, паддинги карточек и формы.
Тело текста осталось 16px: читаемость важнее плотности.

Требование дизайн-системы Amex: цель для пальца не меньше 44px. Все ссылки и
кнопки, включая подвал, получили `min-h-11 sm:min-h-0` — на телефоне это цель для
пальца, на десктопе для мыши. В подвале при этом убраны вертикальные отступы между
ссылками (`flex flex-col sm:gap-3`): 44px высоты сами работают разделителем.
Проверяется скриптом: на 375px список целей ниже 44px должен быть пустым.

Правила, которых держимся на этой странице:
- Радиусы: кнопки и поля 4px, карточки и фото 8px. Других значений нет.
- Шрифт `Benton Sans` лицензионный, свободной веб-версии нет: стек честно откатывается
  на Helvetica Neue / Helvetica / Arial (кириллица есть у обоих).
- Тема одна, светлая. Тёмно-синие полосы — поверхность той же системы, а не второй режим.
- Форма запроса пока **никуда не отправляется**: валидация и экран «Заявка принята»
  работают локально, бэкенда нет.

## ВАЖНО: синхронизация с Google AI Studio
Проект параллельно живёт в AI Studio (https://ai.studio/apps/4abf7e93-624e-4e29-a3ae-55e5da77e728).
Пуш из AI Studio **перезаписывает** репозиторий своим снапшотом: он уже один раз снёс
`package-lock.json`, `scripts/deploy.sh`, `PROGRESS.md`, `base` в `vite.config.ts`, title в `index.html`
и логотипы (коммит 1517f15). Правки были восстановлены ручным ребейзом.

Порядок работы, чтобы не терять код:
1. Перед правками в AI Studio — залить туда актуальную версию из репозитория
   (архив с логотипами: `~/Downloads/euroband-with-logo.zip`, пересобирается из текущего состояния).
2. После пуша из AI Studio — проверять `git log` и восстанавливать конфиг публикации, если он снесён.
3. Не редактировать один и тот же файл одновременно в репозитории и в студии.

## Сделано
- Настроена публикация на GitHub Pages: `base: '/invit/'` для прод-сборки, деплой скриптом в ветку `gh-pages`, Pages переключены на `gh-pages` (legacy build).
- `index.html`: `lang="ru"`, корректный title/description, favicon из знака ИНВИТ.
- Логотипы из `.ai` (в `brand/`) конвертированы в SVG: `src/assets/logo/{invit,euroband}-{color,mono}.svg`. Mono-версии выводятся на тёмном фоне через `brightness-0 invert`.
- Фирменная палитра из логотипов вынесена в `@theme` (`src/index.css`): `brand-blue`, `brand-sky`, `brand-navy`, `brand-graphite`, `brand-red`, `brand-red-light`. Все хардкод-хексы (`#0B5FA5`, `#F39200`, …) заменены на токены; оранжевый акцент заменён на красный EUROBAND.
- Логотипы встроены в Header (цветные) и Footer (белые) вместо текстовых заглушек.
- Мессендж переориентирован на «белорусский производитель уплотнительных и герметизирующих лент EUROBAND»: топбар, hero-слайд 1, блок преимуществ, секция «О компании», футер.
- Тексты и контакты выверены по invit.by: описание компании, два офиса (Минск / Солигорск) с реальными телефонами и графиком, e-mail, «с 2009».
- Убраны выдуманные данные: УНП 191234567, номер сертификата БелТПП №2025/11, «гарантия 10 лет», «3500+ км ленты/мес», «склад 1500 м²».

## Новости и фото
Новости выгружены с invit.by/news/ — 6 деловых материалов с их же изображениями.
Поздравления с праздниками (2014–2016) и две акции 2015 года с ценами не переносил:
устарели. Стоковых unsplash-фото на сайте больше нет.

Реальных фотографий производства у клиента на сайте нет — в блоке «О компании»
стоят фото продукции EUROBAND с invit.by. Если клиент пришлёт снимки цеха и склада,
менять здесь: `src/components/ManufacturingSection.tsx`, вкладка «О компании & История».

## Мобильная версия
Проверено на 375px. Что сделано отдельно под телефон:
- шапка: поиск переехал в бургер-меню (в строке шапки для него нет места), в топбаре
  остаётся только телефон;
- герой: кнопки на всю ширину, уменьшенные отступы;
- раздел каталога: вместо бокового списка подразделов — прокручиваемая лента чипов,
  товары начинаются сразу под заголовком (`CategoryPage`, блок `lg:hidden`);
- вкладки в блоке о производстве прокручиваются горизонтально;
- длинные названия в плитках переносятся (`hyphens-auto break-words`);
- форма поиска заказа: кнопка под полем, а не поверх него.
Таблицы в описании товара скроллятся внутри своего контейнера — страница по горизонтали
не едет ни на одной странице.

## Следующие шаги
- Дождаться правок клиента (он пока не определился с направлением).
- Изображения товаров сейчас отдаются с invit.by по прямым ссылкам. При переносе на боевой
  хостинг — выгрузить их локально (или оставить, если сайт будет жить на том же домене).
- Артикулов на invit.by нет, поэтому адрес товара строится из slug страницы-источника.
  Если появится номенклатура с артикулами — добавить поле `code` и показывать его в карточке.
- Проверить у клиента: «порезка 10–1500 мм», «класс герметичности В и С», тексты сертификатов.
- Сертификаты: 10 реальных сканов из галереи invit.by (Документы -> Сертификаты).
  Срок действия и орган выдачи не показываем — на сайте их нет, а на скане
  сертификата собственного производства стоит 2022–2023 (по новостям обновлён до 2025–2027).
  Если клиент даст свежие сканы и реквизиты — заполнить `issuedBy` и `validUntil`.

## Ключевые решения (и почему)
- Деплой через ветку `gh-pages`, а не GitHub Actions: у токена `gh` нет scope `workflow`, пуш `.github/workflows/*` отклоняется. Чтобы включить CI-деплой: `gh auth refresh -s workflow`, затем вернуть workflow-файл.
- Основной синий оставлен `#0B5FA5` (не логотипный `#3D90CD`): светлый логотипный синий не даёт контраста с белым текстом на плашках. Логотипный синий используется как `brand-sky` для акцентов.
- Красный `#D91920` (из EUROBAND) вместо оранжевого `#F39200`: оранжевого нет в бренде, плюс с белым текстом он давал контраст ~2.4:1 (красный — 5.1:1).
- SVG-логотипы получены кропом viewBox из `.ai` (PDF) + удалением путей вне viewBox — файлы уменьшились с 71K до 18K.
- Роутинг на `react-router-dom` с обычными URL (не хэш) — ради SEO. На GitHub Pages прямые ссылки
  работают через `404.html` (копия `index.html`, делается в `scripts/deploy.sh`); статус ответа при
  этом 404, поэтому на боевом хостинге invit.by нужно настроить обычный SPA-rewrite на `index.html`.
- `basename` роутера берётся из `import.meta.env.BASE_URL`, поэтому один и тот же код работает и на
  `/` в разработке, и на `/invit/` на GitHub Pages.

## Реквизиты (подтверждены клиентом)
- ООО «ИНВИТ», УНП **192436058**
- Юридический адрес: г. Минск, ул. Мясникова, 78, оф. 6
- Выводятся в подвале и на странице контактов, блок «Реквизиты».
- Расчётного счёта пока нет — место под него размечено комментарием в `Footer.tsx`.

## Открытые вопросы
- Актуальный адрес: на invit.by в шапке «г. Минск, ТЦ Сеница, оф. 9», в новостях — переезд на МКАД. Что показывать основным?
- Нужен ли раздел «Солигорск» на видном месте (сейчас только в футере)?
